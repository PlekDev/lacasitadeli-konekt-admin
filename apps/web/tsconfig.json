const fs = require('fs');
const t = JSON.parse(fs.readFileSync('tsconfig.json','utf8'));
t.compilerOptions.ignoreDeprecations = '5.0';
t.compilerOptions.moduleResolution = 'bundler';
fs.writeFileSync('tsconfig.json', JSON.stringify(t, null, 2));
"